apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  labels:
    app: weather
    tier: frontend
spec:
  type: LoadBalancer
  ports:
  - port: 9000
    targetPort: 9000
  selector:
    app: weather
    tier: frontend
